import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />); //components는 꼭 대문자로 시작해야 됨 태그가 대문자로 시작? 아 태그가 아니라 components구나

